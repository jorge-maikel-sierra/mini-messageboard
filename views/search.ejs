<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f0f2f5;
      color: #333;
      padding: 2rem;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2rem;
    }

    h1 { font-size: 2rem; color: #1a1a2e; }

    .search-bar {
      display: flex;
      gap: 0.6rem;
      margin-bottom: 1.5rem;
    }

    .search-bar input[type="text"] {
      flex: 1;
      padding: 0.65rem 0.9rem;
      border: 1.5px solid #d1d5db;
      border-radius: 7px;
      font-size: 1rem;
      outline: none;
    }

    .search-bar input[type="text"]:focus {
      border-color: #4f46e5;
    }

    .btn {
      display: inline-block;
      padding: 0.65rem 1.2rem;
      border-radius: 7px;
      font-weight: 600;
      font-size: 0.95rem;
      cursor: pointer;
      text-decoration: none;
      transition: background-color 0.2s ease;
      border: none;
    }

    .btn-primary { background-color: #4f46e5; color: #fff; }
    .btn-primary:hover { background-color: #4338ca; }
    .btn-secondary { background-color: #e5e7eb; color: #374151; }
    .btn-secondary:hover { background-color: #d1d5db; }

    .results-meta {
      font-size: 0.9rem;
      color: #6b7280;
      margin-bottom: 1.2rem;
    }

    .results-meta strong { color: #4f46e5; }

    .messages-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    .message-card {
      background-color: #fff;
      border-radius: 10px;
      padding: 1.25rem 1.5rem;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.08);
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      gap: 1rem;
    }

    .message-info { flex: 1; }

    .message-user {
      font-weight: 700;
      color: #4f46e5;
      margin-bottom: 0.3rem;
    }

    .message-text {
      font-size: 1rem;
      color: #111827;
      margin-bottom: 0.5rem;
    }

    .message-date { font-size: 0.8rem; color: #9ca3af; }

    .empty-state {
      text-align: center;
      padding: 3rem;
      color: #9ca3af;
      font-size: 1.1rem;
    }
  </style>
</head>
<body>

  <header>
    <h1>🔍 <%= title %></h1>
    <a href="/" class="btn btn-secondary">← Volver al inicio</a>
  </header>

  <form action="/search" method="GET" class="search-bar">
    <input
      type="text"
      name="q"
      placeholder="Buscar por usuario o mensaje..."
      value="<%= q %>"
      autocomplete="off"
    />
    <button type="submit" class="btn btn-primary">Buscar</button>
  </form>

  <% if (q) { %>
    <p class="results-meta">
      <% if (results.length > 0) { %>
        Se encontraron <strong><%= results.length %></strong> resultado(s) para "<strong><%= q %></strong>".
      <% } else { %>
        No se encontraron resultados para "<strong><%= q %></strong>".
      <% } %>
    </p>
  <% } %>

  <% if (results.length > 0) { %>
    <div class="messages-list">
      <% results.forEach(function(message) { %>
        <div class="message-card">
          <div class="message-info">
            <p class="message-user">👤 <%= message.user %></p>
            <p class="message-text"><%= message.text %></p>
            <p class="message-date">🕒 <%= message.added.toLocaleString("es-ES") %></p>
          </div>
          <a href="/messages/<%= message.id %>" class="btn btn-secondary">Abrir</a>
        </div>
      <% }); %>
    </div>
  <% } else if (q) { %>
    <div class="empty-state">
      <p>😕 Sin resultados. Intenta con otro término.</p>
    </div>
  <% } %>

</body>
</html>
